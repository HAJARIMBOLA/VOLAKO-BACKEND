# Local development profile: run `docker compose up -d` (see docker-compose.yml) then
# start the app with SPRING_PROFILES_ACTIVE=dev
DB_URL=jdbc:postgresql://localhost:5433/volako
DB_USERNAME=volako
DB_PASSWORD=volako
JWT_SECRET=dev-only-secret-do-not-use-in-production-0123456789abcdef
FRONTEND_URL=http://localhost:3000

spring.jpa.show-sql=false
logging.level.com.volako.backend=DEBUG
